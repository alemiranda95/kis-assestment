<resources>
    <!--
      Base window theme for a Compose app. Compose drives the real Material 3
      theming (see KisTheme); this XML theme only provides a no-action-bar
      DayNight window. Platform parent avoids depending on the Material View library.
    -->
    <style name="Theme.KISTechnicalAssestment" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
